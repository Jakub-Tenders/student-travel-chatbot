| Member | Task | Description | Decisions Made | Date |
|--------|------|-------------|----------------|------|
| Charles | Flight Search Integration | Integrated flight search into the backend via Sky-Scrapper (RapidAPI) and wired it to the chat endpoint | Quite a few decisions made on this one: the initial plan was to use the Amadeus API, as that had the greatest history and industry backing. However, this API became unavailable to the general public recently and can no longer be used. Subsequently, I found an API that uses a web scraper available on RapidAPI (Sky Scrapper). After developing with it, I quickly ran out of tokens. The extensive, usable implementation for the Sky Scrapper integration exists, but we ended up having to use mock data due to token usage and rate limiting. | 24/06/2026 |
| Charles | Prompt Builder + LLM context wiring | Implemented prompt_builder.py, wired flights, rides, and hostels to the LLM chat context. Also did some debugging at this stage. | We chose to inject travel data before the actual exchange (here, we used a fake prior exchange to simulate the context for the LLM). Furthermore, we progressively added each element to the LLM and measured its response to each increment. | 25/06/2026 |
| Charles | Security Hardening & Code Cleanup | Added rate limiting and CORS restrictions to the Flask backend, removed debug logs that were leaking API keys, and fixed a data mutation bug in the mock flight service | Adding a rate limiter for several reasons: notably to avoid malicious actions and preserve token usage. We locked CORS from open wildcard to our specific origins. | 26/06/2026 |